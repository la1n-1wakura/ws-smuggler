# WS-Smuggler 🛡️⛓️

**WS-Smuggler** — это специализированный кроссплатформенный инструмент (CLI-утилита) для анализа защищенности и проведения тестирования на проникновение (Pentesting) WebSocket-соединений. Инструмент сфокусирован на выявлении уязвимостей класса **WebSocket Frame Smuggling** (смешение кадров) в инфраструктуре веб-приложений.

> 🎓 *Данный проект разработан в рамках практической части выпускной квалификационной работы (дипломного проекта).*

---

## 🔬 Архитектура и Структура проекта

Проект спроектирован по модульному принципу. Для обеспечения полного контроля над структурой сетевых пакетов и манипуляции битами кадров (RFC 6455) логика утилиты построена на использовании низкоуровневых системных сокетов.

```text
ws-smuggler/
│
├── 01_config/                  # Шаблоны атак и заготовки Payload (JSON)
├── 02_environment/             # Docker-стенд (Proxy + Backend) для демонстрации уязвимости
├── 03_dumps/                   # Дампы сетевого трафика (Hex, PCAP)
├── 04_reports/                 # Результаты сканирования (JSON, HTML)
│
├── 05_src/                     # Исходный код утилиты
│   ├── main.py                 # Точка входа (Парсер CLI аргументов)
│   ├── core/                   # Ядро системы (Низкоуровневая логика)
│   │   ├── connection.py       # Управление TCP/SSL/Handshake сокетами
│   │   ├── frame_builder.py    # Сборщик и "ломатель" кадров RFC 6455
│   │   └── traffic_logger.py   # Модуль записи сырого трафика (Hex/PCAP)
│   ├── modules/                # Режимы работы
│   │   ├── manual.py           # Интерактивный конструктор кадров
│   │   └── automated.py        # Сценарии автоматических тестов (Fuzzing)
│   └── ui/                     # Пользовательский интерфейс (TUI)
│       ├── logger.py           # Форматированный вывод (Rich)
│       ├── reporter.py         # Модуль экспорта отчетов
│       └── views.py            # Интерактивные меню и панели
│
├── 06_tests/                   # Модульные тесты (Unit testing)
├── requirements.txt            # Зависимости (rich, prompt_toolkit)
└── README.md                   # Инструкция по запуску
```

---

## 🛠️ Технологический Стек

* **Язык разработки:** Python 3.11+
* **Сетевой уровень:** `socket`, `ssl`
* **Интерфейс (TUI):** 
  * `rich` — визуальное оформление таблиц, логов и Hex-дампов.
  * `prompt_toolkit` — интерактивный ввод с поддержкой истории и автодополнения.
* **Автоматизация CLI:** `click` / `argparse`

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория и подготовка
```bash
git clone https://github.com/la1n-1wakura/utility.git
cd ws-smuggler
```

### 2. Настройка окружения
Рекомендуется использовать виртуальное окружение:
Linux:
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 3. Запуск утилиты
Интерактивный режим:
```bash
python3 src/main.py
```

---

## 📊 Функциональные возможности

* **Низкоуровневый конструктор (Manual Mode):** Возможность вручную выставлять биты `FIN`, `Opcode`, управлять флагом маскирования (`Mask`), а также намеренно искажать длину Payload в заголовке кадра для проверки реакций прокси-серверов.
* **Автоматизированные сценарии (Automated Mode):** Готовые тест-кейсы для симуляции атак десинхронизации (например, внедрение скрытого кадра внутрь Payload легитимного сообщения).
* **Контроль трафика:** Логирование отправленных аномальных байт в формате Hex-дампа и экспорт сессий в `.pcap` для последующего анализа в Wireshark.
* **Отчетность:** Выгрузка структурированных отчетов в формате JSON/HTML для интеграции с процессами DevSecOps.

---

## ⚠️ Disclaimer (Дисклеймер)

*Программа создана исключительно в образовательных целях в рамках академического проекта и предназначена для легального аудита безопасности (Penetration Testing). Автор не несет ответственности за любой ущерб, причиненный с использованием данного инструмента.*
